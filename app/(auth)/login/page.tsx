/**
 * Login page (Phase 0 placeholder).
 *
 * Functional Firebase email/password + Google sign-in arrives in Phase 1.
 */
export default function LoginPage() {
  return (
    <div className="rounded-lg border border-zinc-200 p-6 dark:border-zinc-800">
      <h1 className="text-2xl font-semibold">Sign in</h1>
      <p className="mt-2 text-sm text-zinc-600 dark:text-zinc-400">
        Auth UI lands in Phase 1. For now, this route is here to verify the
        <code className="mx-1 rounded bg-zinc-100 px-1 dark:bg-zinc-800">(auth)</code>
        route group renders.
      </p>
    </div>
  );
}
